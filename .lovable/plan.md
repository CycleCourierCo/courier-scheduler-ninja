

## Change Return Button Label

**File: `src/pages/CustomerOrderDetail.tsx`** (line 236)

Change the button text from `"Return"` to `"Return this bike to sender"` (and the loading state from `"Creating..."` to `"Creating return..."`).

