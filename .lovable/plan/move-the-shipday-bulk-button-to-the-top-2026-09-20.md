# Move the Shipday bulk button to the top

## What is happening

The button was added below the filter controls, so it is farther down the page and not visible in the screenshot. This does not match the request to place it at the top.

## Change

- Move **Add all missing (count)** into the Route Builder header, beside the job totals and refresh control.
- Keep it visible and readable on mobile by placing it on its own full-width row beneath the Route Builder title when space is limited.
- Keep the existing red-cross-only count, disabled state, confirmation, progress, and recheck behaviour unchanged.
- Remove the lower duplicate so there is only one bulk-add button.